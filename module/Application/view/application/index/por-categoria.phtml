<?php 
$title = 'Productos - ' . $categoria->nombre;
$breadcrumb = [
    ['text' => 'Productos', 'url' => '/', 'active' => false],
    ['text' => 'Categorías', 'url' => '/categorias', 'active' => false],
    ['text' => $categoria->nombre, 'url' => '', 'active' => true]
];
?>

<div class="animate-slide-in">
    <!-- Header con información de la categoría -->
    <div class="view-header-actions" style="margin-bottom: 25px;">
        <div style="display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 15px;">
            <div>
                <h2 style="margin: 0; color: var(--text-color); display: flex; align-items: center; gap: 10px;">
                    <i class="fas <?php 
                        $icons = [
                            'Electrónicos' => 'fa-mobile-alt',
                            'Ropa' => 'fa-tshirt',
                            'Hogar' => 'fa-home',
                            'Deportes' => 'fa-dumbbell',
                            'Libros' => 'fa-book',
                            'Salud' => 'fa-heartbeat'
                        ];
                        echo $icons[$categoria->nombre] ?? 'fa-tag';
                    ?>" style="color: var(--primary-color);"></i>
                    <?= htmlspecialchars($categoria->nombre) ?>
                </h2>
                <p style="margin: 8px 0 0 0; color: var(--text-secondary); font-size: 0.95em;">
                    <?= htmlspecialchars($categoria->descripcion) ?>
                </p>
            </div>
            <div style="display: flex; gap: 10px; flex-wrap: wrap;">
                <a href="/categorias" class="btn-modern btn-secondary-modern">
                    <i class="fas fa-arrow-left"></i> Volver a Categorías
                </a>
                <a href="/" class="btn-modern btn-info-modern">
                    <i class="fas fa-list"></i> Todos los Productos
                </a>
                <a href="/productos/create" class="btn-modern btn-primary-modern">
                    <i class="fas fa-plus"></i> Nuevo Producto
                </a>
            </div>
        </div>
    </div>

    <?php if (empty($productos)): ?>
        <!-- Estado vacío para categoría sin productos -->
        <div class="modern-card">
            <div class="empty-state">
                <div class="empty-state-icon">
                    <i class="fas fa-box"></i>
                </div>
                <h3>No hay productos en esta categoría</h3>
                <p>
                    Aún no hay productos registrados en la categoría "<?= htmlspecialchars($categoria->nombre) ?>".
                </p>
                <a href="/productos/create" class="btn-modern btn-primary-modern">
                    <i class="fas fa-plus"></i> Agregar Primer Producto
                </a>
            </div>
        </div>
    <?php else: ?>
        <!-- Tabla de productos -->
        <div class="modern-card">
            <div class="modern-card-header">
                <h3>
                    <i class="fas fa-list"></i> 
                    Productos en <?= htmlspecialchars($categoria->nombre) ?>: <?= count($productos) ?>
                </h3>
            </div>
            
            <div class="modern-card-body" style="padding: 0;">
                <div style="overflow-x: auto;">
                    <table class="modern-table">
                        <thead>
                            <tr>
                                <th style="min-width: 80px;">ID</th>
                                <th style="min-width: 250px;">Producto</th>
                                <th style="min-width: 120px;">Código</th>
                                <th style="min-width: 150px;">Email</th>
                                <th style="min-width: 120px;">Fecha Lanz.</th>
                                <th style="min-width: 120px;">Precio</th>
                                <th style="min-width: 100px;">Descuento</th>
                                <th style="min-width: 150px;">Acciones</th>
                            </tr>
                        </thead>
                        <tbody>
                            <?php foreach ($productos as $producto): ?>
                                <tr class="table-row-hover">
                                    <td><strong><?= $producto->id ?></strong></td>
                                    <td>
                                        <div style="display: flex; align-items: center;">
                                            <?php if ($producto->imagen): ?>
                                                <div style="width: 50px; height: 50px; margin-right: 12px; overflow: hidden; border-radius: 8px; border: 2px solid #e8f5e8;">
                                                    <img src="/imagen/<?= $producto->id ?>" 
                                                         alt="<?= htmlspecialchars($producto->nombre) ?>"
                                                         style="width: 100%; height: 100%; object-fit: cover; cursor: pointer;"
                                                         onclick="openImagePreview('/imagen/<?= $producto->id ?>', '<?= htmlspecialchars($producto->nombre) ?>')">
                                                </div>
                                            <?php else: ?>
                                                <div style="width: 50px; height: 50px; background: #f8f9fa; border-radius: 8px; display: flex; align-items: center; justify-content: center; margin-right: 12px; border: 2px solid #dee2e6;">
                                                    <i class="fas fa-box" style="color: #95a5a6; font-size: 20px;"></i>
                                                </div>
                                            <?php endif; ?>
                                            <div>
                                                <div style="font-weight: 600; color: var(--text-color); margin-bottom: 2px;">
                                                    <?= htmlspecialchars($producto->nombre) ?>
                                                </div>
                                                <div style="font-size: 0.85em; color: var(--text-secondary);">
                                                    <i class="fas fa-tag" style="margin-right: 4px;"></i>
                                                    <?= htmlspecialchars($categoria->nombre) ?>
                                                </div>
                                            </div>
                                        </div>
                                    </td>
                                    <td>
                                        <code style="background: var(--background-light); padding: 4px 8px; border-radius: 4px; font-size: 0.9em;">
                                            <?= htmlspecialchars($producto->codigo) ?>
                                        </code>
                                    </td>
                                    <td>
                                        <a href="mailto:<?= htmlspecialchars($producto->emailContacto) ?>" 
                                           style="color: var(--primary-color); text-decoration: none;">
                                            <?= htmlspecialchars($producto->emailContacto) ?>
                                        </a>
                                    </td>
                                    <td>
                                        <div style="display: flex; align-items: center; gap: 6px;">
                                            <i class="fas fa-calendar" style="color: var(--text-secondary); font-size: 0.85em;"></i>
                                            <?= date('d/m/Y', strtotime($producto->fechaLanzamiento)) ?>
                                        </div>
                                    </td>
                                    <td>
                                        <div style="font-weight: 600; color: var(--success-color); font-size: 1.1em;">
                                            S/ <?= number_format($producto->precio, 2) ?>
                                        </div>
                                    </td>
                                    <td>
                                        <?php if ($producto->descuentoPorcentaje > 0): ?>
                                            <span style="background: var(--accent-color); color: white; padding: 4px 8px; border-radius: 4px; font-size: 0.85em; font-weight: 600;">
                                                <?= number_format($producto->descuentoPorcentaje, 1) ?>% OFF
                                            </span>
                                        <?php else: ?>
                                            <span style="color: var(--text-secondary); font-size: 0.9em;">Sin descuento</span>
                                        <?php endif; ?>
                                    </td>
                                    <td>
                                        <div class="action-buttons">
                                            <a href="/productos/view/<?= $producto->id ?>" 
                                               class="btn-modern btn-info-modern" 
                                               title="Ver detalles"
                                               style="padding: 6px 8px;">
                                                <i class="fas fa-eye"></i>
                                            </a>
                                            <a href="/productos/edit/<?= $producto->id ?>" 
                                               class="btn-modern btn-warning-modern" 
                                               title="Editar"
                                               style="padding: 6px 8px;">
                                                <i class="fas fa-edit"></i>
                                            </a>
                                            <a href="/productos/delete/<?= $producto->id ?>" 
                                               class="btn-modern btn-danger-modern delete-btn" 
                                               title="Eliminar producto"
                                               style="padding: 6px 8px;"
                                               data-product-name="<?= htmlspecialchars($producto->nombre) ?>">
                                                <i class="fas fa-trash"></i>
                                            </a>
                                        </div>
                                    </td>
                                </tr>
                            <?php endforeach; ?>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    <?php endif; ?>
</div>

